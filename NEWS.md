# timesfm 0.1.0

* Initial release.
* `timesfm()` produces zero-shot point and decile forecasts of univariate
  time series using Google Research's TimesFM 2.5 (200M) foundation model.
* `install_timesfm()` performs one-time setup of the underlying Python
  package into a dedicated virtualenv, requiring Python >= 3.10.
