% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timesfm.R
\name{timesfm}
\alias{timesfm}
\title{Forecast a time series with TimesFM}
\usage{
timesfm(y, horizon)
}
\arguments{
\item{y}{A numeric vector or \code{ts} object: the observed series, oldest
first. Series longer than 1024 points are used from the most recent
1024.}

\item{horizon}{Number of future steps to forecast, between 1 and 256.}
}
\value{
An object of class \code{"timesfm"}: a list with elements
\itemize{
\item \code{mean} — numeric vector of point forecasts, length \code{horizon}.
\item \code{quantiles} — numeric matrix with \code{horizon} rows and columns
\code{q10} ... \code{q90}: the deciles of the forecast distribution.
\item \code{horizon} — the forecast length.
}
}
\description{
\code{timesfm()} produces a zero-shot probabilistic forecast of a univariate
time series using the pretrained TimesFM 2.5 (200M) foundation model.
There is no training step: the model reads your series as context and
forecasts in a single forward pass.

Loading the pretrained weights is the slow part, so it happens once per
session and the model is reused by later calls.
}
\examples{
\dontrun{
fc <- timesfm(AirPassengers, horizon = 24)
fc

fc$mean
fc$quantiles[, c("q10", "q90")]  # an 80\% interval
}
}
